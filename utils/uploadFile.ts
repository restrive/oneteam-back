import fs from 'fs';
import { cLog } from './logger';
/*
doc function dynamically checks base 64's and writes them to a a use folder and their book id folder within
*/
async function docs(files: any, userId: string | number, localBookID: string | number, internationaBookID: string | number) {

    return false;
}

export = {
    docs
}